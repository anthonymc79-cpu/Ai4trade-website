{
  "name": "ai4trade-payments",
  "version": "1.0.0",
  "private": true,
  "description": "Server-side Stripe payments API for AI4Trade (holds secret keys, computes amounts server-side, verifies webhooks)",
  "main": "payments-server.js",
  "scripts": {
    "start": "node payments-server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "stripe": "^16.9.0"
  }
}
